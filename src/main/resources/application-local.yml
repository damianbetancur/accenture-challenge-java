app:
  name: "@project.name@"
  version: "@project.version@"

spring:
  application:
    name: accenture-challenge-java
    profiles:
      active: local
logging:
  level:
    root: info

data:
  customers:
    file: data/customers.json
  products:
    file: data/products.json

server:
  port: 8082

